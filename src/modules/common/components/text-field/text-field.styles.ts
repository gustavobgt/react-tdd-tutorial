import styled from 'styled-components';

const Wrapper = styled.div`
	display: inline-flex;
	flex-direction: column;
	position: relative;
	padding: 0;
	margin: 0;
	border: 0;
`;

export default {
	Wrapper,
};
